fastapi>=0.111.0
uvicorn>=0.29.0
python-multipart>=0.0.9
pydantic>=2.7.1

# Use only the newer google-genai SDK (without [grpc] extra to avoid grpcio-status conflict)
google-genai==1.15.0
google-generativeai==0.8.3

# Google dependencies - version locked for stability
google-api-core==2.27.0
googleapis-common-protos==1.63.0
google-auth==2.29.0
# Use protobuf 3.19.6 - compatible with all dependencies
protobuf==3.19.6

PyJWT==2.8.0

# Computer vision — local video analysis (CV phase of the hybrid pipeline)
# mediapipe 0.10.11 requires protobuf<4
mediapipe==0.10.11
opencv-python==4.10.0.84
requests==2.31.0

# Async task queue
celery==5.4.0
redis==5.0.4

# Audio processing
librosa==0.10.2
soundfile==0.12.1
audioread==3.0.1
